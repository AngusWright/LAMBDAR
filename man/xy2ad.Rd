\name{xy2ad}
\alias{xy2ad}
%- Also NEED an '\alias' for EACH other topic documented here.
\title{
Pixel to RA/Dec via Header Astrometry
}
\description{
Converts Pixel locations x,y into RA and Dec positions by reading the necessary astrometry parameters
from an astrometry structure, using the TAN Gnomonic coordinate system.
The astrometry structure can be generated by using the read.astr procedure.
}
\usage{
xy2ad(x, y, astr_struc, diagnostic = FALSE)
}
%- maybe also 'usage' for other objects documented here.
\arguments{
  \item{x}{
numeric (n) vector; x-coordinates (pixel number)
}
  \item{y}{
numeric (n) vector; y-coordinates (pixel number)
}
  \item{astr_struc}{
list; Astrometry from FITS header relevant to these pixel locations
}
  \item{diagnostic}{
logical; if TRUE, write diagnostic output to 'message'
}
}
%%\details{
%%  ~~ If necessary, more details than the description above ~~
%%}
\value{
numeric (n,2) vector; The RA and Dec vectors (column bound) of each of the pixel locations provided
}
%%\references{
%% ~put references to the literature/web site here ~
%%}
\author{
Angus H Wright{ ICRAR}
 {\email{angus.wright@icrar.org}}
}
%%\note{
%%  ~~further notes~~
%%}

%% ~Make other sections like Warning with \section{Warning }{....} ~

\seealso{
MeasureFluxes, read.astr
}
\examples{

}
% Add one or more standard keywords, see file 'KEYWORDS' in the
% R documentation directory.
\keyword{ ~kwd1 }
\keyword{ ~kwd2 }% __ONLY ONE__ keyword per line
